syntax = "proto3";

package run;

import "google/protobuf/any.proto";

service RunService {
    rpc Run(RunRequest) returns (RunResponse) {}
}

message RunRequest {
    string functionName = 1;
    string input = 2;
}

message RunResponse {
    string output = 1;
}
