syntax = "proto3";

package protoflow;

service {{index . "Runtime"}}Service {
    {{- range index . "Methods"}}
    rpc {{. -}}(Data) returns (Data);
    {{- end}}
}

message Data {
    string value = 1;
}
